import { ModeToggle } from '@/components/mode-toggle.tsx';

export const Header = () => {
  return (
    <header className='flex justify-between p-4'>
      <div />
      <ModeToggle />
    </header>
  );
};
